# Results

This folder keeps raw experiment logs that support the training and long-context
claims in the repo.

- `training/` contains end-to-end and tank-style training reports.
- `long_context/` contains the ctx=512/1024 long-context runs and formula sweeps.

The goal is to keep paper evidence versioned without cluttering the repo root.
